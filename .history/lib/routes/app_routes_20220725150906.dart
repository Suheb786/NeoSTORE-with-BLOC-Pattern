import 'package:flutter/material.dart';

class Routes {
  onGenerate(RouteSettings routeSettings) {
    switch (routeSettings.name) {
      case Routes.LOGIN:
        break;
      default:
    }
  }
}
